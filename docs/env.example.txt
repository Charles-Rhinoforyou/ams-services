# ============================================================
#  .env.example — Variables d'environnement (SANS secret réel)
#  Le site est statique + PHP sur OVH. Ces variables ne sont
#  utilisées QUE côté serveur (jamais envoyées au navigateur).
#  Copiez ce fichier en .env (NON committé) une fois les
#  intégrations serveur activées. Aujourd'hui : non requis.
# ============================================================

# --- Google Search Console (Phase 6, non activé) ---
# Compte de service Google avec accès à la propriété Search Console.
GSC_CLIENT_EMAIL=
GSC_PRIVATE_KEY=
GSC_SITE_URL=https://ams-services-depannage.fr/

# --- Matomo (option analytics avancée, non activée) ---
# Si vous installez Matomo auto-hébergé sur OVH.
MATOMO_URL=
MATOMO_TOKEN=
MATOMO_SITE_ID=

# NB : le mot de passe admin n'est PAS ici. Il est stocké en
# SHA-256 directement dans les scripts PHP (api/_common.php,
# publier-realisations.php, uploader-image.php).
